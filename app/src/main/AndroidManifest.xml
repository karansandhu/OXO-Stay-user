<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.user.oxostay">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screens.ReviewActivity"></activity>
        <activity android:name=".screens.PaymentActivity" />
        <activity android:name=".screens.ImageViewActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".screens.MapssActivity"
            android:label="@string/title_activity_mapss" />
        <activity android:name=".screens.MapsActivity" />
        <activity android:name=".screens.CovidGuidelinesActivity" />
        <activity android:name=".screens.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.OTPActivity" />
        <activity android:name=".screens.ReferralActivity" />
        <activity android:name=".screens.OffersActivity" />
        <activity android:name=".screens.WalletActivity" />
        <activity android:name=".screens.ChangePassActivity" />
        <activity android:name=".screens.TermsActivity" />
        <activity android:name=".screens.PrivacyActivity" />
        <activity android:name=".screens.HotelDetailActivity" />
        <activity android:name=".screens.HotelListActivity" />
        <activity android:name=".screens.SearchActivity" />
        <activity android:name=".screens.LocationActivity" />
        <activity android:name=".screens.SignupActivity" />
        <activity android:name=".screens.LoginActivity" />
        <activity android:name=".screens.MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" /> <!-- <meta-data -->
        <!-- android:name="com.google.android.maps.v2.API_KEY" -->
        <!-- android:value="AIzaSyCsK9ntiS3JE0ZLsY4N25fmH9-5Pt2sG8o" /> -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>